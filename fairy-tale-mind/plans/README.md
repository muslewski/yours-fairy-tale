# Plans

Writing-plans live here. Convention: `YYYY-MM-DD-<name>.md`.

Each plan contains: goal, ordered task list, branch name, acceptance criteria. Plans are produced from a spec; implementation follows the plan. Archive (never delete) after the branch merges.
