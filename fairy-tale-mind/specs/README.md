# Specs

Design specs originate from brainstorming sessions. Convention: `YYYY-MM-DD-<name>-design.md`.

Each spec captures: problem statement, constraints, proposed approach, open questions. Specs are immutable once a plan is written from them — supersede with a new spec, never edit in place.
